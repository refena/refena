name: riverpie_flutter
description: A tiny state management library for Dart and Flutter. Inspired by Riverpod.
version: 0.10.0
repository: https://github.com/Tienisto/riverpie
topics:
  - state
  - provider
  - riverpod
  - framework

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  riverpie: ^0.9.0
  meta: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.0.0

flutter:
