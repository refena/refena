name: example
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dio: 5.3.2
  flutter:
    sdk: flutter
  refena_flutter: ^0.41.0
  refena_inspector_client: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  refena_inspector: ^0.8.0

flutter:
  uses-material-design: true
