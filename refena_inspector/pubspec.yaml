name: refena_inspector
description: The inspector for the refena library.
version: 0.0.1
repository: https://github.com/refena/refena
topics:
  - refena

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.1.0
  path: ^1.8.0
  refena_flutter: ^0.29.0
  refena_inspector_client: ^0.0.1
  shelf: ^1.4.1
  shelf_web_socket: ^1.0.4
  web_socket_channel: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/img/
